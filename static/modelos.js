(function () { const status=document.getElementById('estadoCopia'); document.querySelectorAll('[data-copy]').forEach(button=>button.addEventListener('click',async()=>{const field=document.getElementById(button.dataset.copy);try{await navigator.clipboard.writeText(field.value);status.textContent='Modelo copiado. Pode agora colá-lo num e-mail ou documento.';}catch(error){field.focus();field.select();status.textContent='Não foi possível copiar automaticamente. O texto ficou selecionado; carregue Control+C.';}})); }());
