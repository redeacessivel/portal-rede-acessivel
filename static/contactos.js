(function () { const input=document.getElementById('pesquisaContactos'); const cards=[...document.querySelectorAll('.contacto')]; const status=document.getElementById('estadoContactos'); const norm=s=>(s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,''); input.addEventListener('input',()=>{const q=norm(input.value.trim());let shown=0;cards.forEach(card=>{const match=!q||norm(card.textContent+' '+card.dataset.termos).includes(q);card.hidden=!match;if(match)shown++;});status.textContent=q?`${shown} contacto${shown===1?'':'s'} encontrado${shown===1?'':'s'}.`:'';}); }());
